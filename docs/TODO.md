# プロジェクト TODO リスト

本プロジェクトの進化、スキルの改善、および未完了タスクの管理表です。

## 1. 最優先：セッション連携プロトコルの形式知化
- [x] **成功したプロトコルの定着**: 「指示書同期・パス検証・報告義務化」の仕組みを、どう実装するのが最適か調査・決定する。 [brainstorming] (2026-03-04 完了)
- [x] **Phase 1: 制御スクリプトの実装**: `gemini-sub` (Python) の高品質な修正実装。 [subagent-driven-development] (2026-03-07 完了)
- [x] **Phase 2: スキル定義**: `session-coordination` スキルの作成とグローバル配置。 [writing-skills] (2026-03-08 完了)
- [ ] **次世代連携プロトコルの検討（権限境界の克服）**: 現在の GPAC プロトコルが抱える「ワークスペース外ディレクトリへの依存」を、**システムグローバルな配置（SSOT）というメリットを維持したまま**、抜本的に解決する設計を検討する。 [brainstorming]
    - **背景**: 現行設計ではセッション管理ディレクトリ（`~/.gemini/sub-sessions/`）が Gemini CLI のセキュリティ制限の外にある。そのためエージェントは `cat`/`cp` といった低レイヤーなシェルコマンドを直接実行せざるを得ず、エスケープミスや初動の迷いが生じている。
    - **検討案**: ワークスペース内への移動（過去に却下済）ではなく、スクリプト側で「エージェント専用の読み書きAPI（コマンド）」を強化する。エージェントがファイルパスや権限境界を意識せず、構造化データ（JSON/YAML）として指示・報告を完結できる、より抽象度の高い通信手段の設計。
- [ ] **Phase 3: 既存スキルへの統合（相互接続）**: 上記の権限境界問題を克服したクリーンな設計に基づき、各スキルの「ハンドオフ（引き渡し）」箇所を修正し、GPACプロトコルを自然に呼び出せるようにする。
    - **writing-plans**: 「別セッションでの実行」の標準手順として `session-coordination` を組み込む。
    - **dispatching-parallel-agents**: 並行作業の管理基盤として GPAC を採用するよう指示を更新。
    - **brainstorming**: 大規模タスクの切り出し判断基準に GPAC の使用を追加。

## 2. 自己改善ループの強化と「謝罪・ミス」の連動
- [ ] **改善トリガーの精緻化**: 「謝罪・ミス」およびサブセッションでの「トラブル・解決策」を、スキルの不備として捉え、改善候補に自動的に挙げるプロセスを確立する。 [brainstorming]
- [ ] **振り返りプロンプトの洗練**: 実装完了後の振り返りにおいて、上記の知見を効率的に引き出す仕組みを作る（エージェント化の検討を含む）。 [writing-skills]

## 3. 運用基盤の改善
- [ ] **ロードマップ管理スキルの創設 (Roadmap Management)**: 
    - **目的**: 長期セッションにおける「今どこにいて、何のためにこのスキルを使っているか」というコンテキストの喪失を防ぐ。
    - **要件**:
        1. 大規模タスクの開始時に、全行程を俯瞰する「ロードマップ（Task 1...N）」を提示する。
        2. 各 Task において「使用するスキル」と「具体的な完了（GREEN）条件」を事前に宣言する。
        3. タスクの変わり目（コミット直後）に、必ず「現在地：Task M / 次：Task M+1（使用予定スキル X）」をユーザーに提示し、コンテキストを同期する。
    - **手法**: `writing-skills` を使用して、進捗管理の「型」をスキルとして定義する。
- [ ] **TODOリストの完全なファイル管理移行**: 再開用プロンプトからリストを排除し、本ファイル（`docs/TODO.md`）を正味のタスクソースとするワークフローの定着。
- [ ] **Node.js への移行ロードマップ策定**: `node:test` と `node:assert` を活用し、標準機能のみで完結するテスト環境を設計する。

## 4. 検証と外部調査
- [ ] **大規模インテグレーションテスト (最終検証)**: 移植・改善した全スキルを横断的に使用する一連のシナリオ（計画策定から実装、自己改善、完了まで）を実行し、システム全体の調和を確認する。
- [ ] **外部プロジェクト（barretstorck/gemini-superpowers等）の調査**: 一括インストール方法や、より最適なディレクトリ構成がないか調査する。

---
*Created by Gemini CLI Agent as part of the session transition.*
